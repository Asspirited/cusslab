{
  "name": "heckler-and-cocks",
  "version": "1.0.0",
  "scripts": {
    "test": "node pipeline/unit-runner.js && node pipeline/gherkin-runner.js",
    "pipeline": "node pipeline/ui-audit.js && node pipeline/browser-sim.js && npm test && node pipeline/coverage.js"
  }
}
